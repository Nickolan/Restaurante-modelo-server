<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reserva Confirmada</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background-color: #121212;
            color: #E0E0E0;
            line-height: 1.6;
            padding: 20px;
        }

        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #1E1E1E;
            border-radius: 16px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            overflow: hidden;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
        }

        .header {
            background: linear-gradient(135deg, #1E1E1E 0%, #2A2A2A 100%);
            padding: 40px 30px;
            text-align: center;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .logo {
            width: 80px;
            height: 80px;
            margin: 0 auto 20px;
            background: linear-gradient(135deg, #FFA500 0%, #FF8C00 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 36px;
            font-weight: bold;
            color: #FFFFFF;
        }

        .header h1 {
            color: #FFFFFF;
            font-size: 28px;
            margin-bottom: 8px;
        }

        .header p {
            color: #B0B0B0;
            font-size: 14px;
        }

        .content {
            padding: 40px 30px;
        }

        .greeting {
            font-size: 18px;
            color: #FFFFFF;
            margin-bottom: 20px;
        }

        .reservation-card {
            background: linear-gradient(135deg, #252525 0%, #2A2A2A 100%);
            border-radius: 16px;
            padding: 30px;
            margin: 30px 0;
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.3);
        }

        .reservation-detail {
            display: flex;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 20px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .reservation-detail:last-child {
            margin-bottom: 0;
            padding-bottom: 0;
            border-bottom: none;
        }

        .detail-icon {
            width: 50px;
            height: 50px;
            background-color: rgba(255, 165, 0, 0.15);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            margin-right: 20px;
            flex-shrink: 0;
        }

        .detail-content {
            flex: 1;
        }

        .detail-label {
            color: #B0B0B0;
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 4px;
        }

        .detail-value {
            color: #FFFFFF;
            font-size: 18px;
            font-weight: 600;
        }

        .detail-value.large {
            font-size: 24px;
            color: #FFA500;
        }

        .info-box {
            background-color: rgba(255, 165, 0, 0.1);
            border-left: 4px solid #FFA500;
            padding: 20px;
            border-radius: 8px;
            margin: 25px 0;
        }

        .info-box p {
            margin: 0;
            color: #E0E0E0;
            font-size: 14px;
            line-height: 1.6;
        }

        .cta-button {
            display: inline-block;
            background: linear-gradient(135deg, #FFA500 0%, #FF8C00 100%);
            color: #FFFFFF;
            text-decoration: none;
            padding: 16px 40px;
            border-radius: 8px;
            font-weight: 600;
            font-size: 16px;
            text-align: center;
            margin: 20px 0;
            transition: transform 0.2s;
            box-shadow: 0 4px 12px rgba(255, 165, 0, 0.3);
        }

        .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 16px rgba(255, 165, 0, 0.4);
        }

        .footer {
            background-color: #1A1A1A;
            padding: 30px;
            text-align: center;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .footer p {
            color: #808080;
            font-size: 13px;
            margin-bottom: 10px;
        }

        .footer a {
            color: #FFA500;
            text-decoration: none;
        }

        @media only screen and (max-width: 600px) {
            .email-container {
                border-radius: 0;
            }

            .content {
                padding: 30px 20px;
            }

            .header {
                padding: 30px 20px;
            }

            .reservation-card {
                padding: 20px;
            }

            .detail-icon {
                width: 40px;
                height: 40px;
                font-size: 20px;
                margin-right: 15px;
            }

            .detail-value {
                font-size: 16px;
            }

            .detail-value.large {
                font-size: 20px;
            }
        }
    </style>
</head>

<body>
    <div class="email-container">
        <div class="header">
            <div class="logo">🍽️</div>
            <h1>¡Reserva Confirmada!</h1>
            <p>Te esperamos pronto</p>
        </div>

        <div class="content">
            <p class="greeting">Hola {{customerName}},</p>
            <p style="margin-bottom: 30px; color: #B0B0B0;">
                Tu reserva ha sido confirmada exitosamente. ¡Estamos emocionados de recibirte!
            </p>

            <div class="reservation-card">
                <div class="reservation-detail">
                    <div class="detail-icon">📅</div>
                    <div class="detail-content">
                        <div class="detail-label">Fecha</div>
                        <div class="detail-value">{{reservationDate}}</div>
                    </div>
                </div>

                <div class="reservation-detail">
                    <div class="detail-icon">🕐</div>
                    <div class="detail-content">
                        <div class="detail-label">Hora</div>
                        <div class="detail-value large">{{reservationTime}}</div>
                    </div>
                </div>

                <div class="reservation-detail">
                    <div class="detail-icon">👥</div>
                    <div class="detail-content">
                        <div class="detail-label">Número de Personas</div>
                        <div class="detail-value">{{numberOfGuests}} {{#if (eq numberOfGuests
                            1)}}persona{{else}}personas{{/if}}</div>
                    </div>
                </div>

                {{#if tableZone}}
                <div class="reservation-detail">
                    <div class="detail-icon">📍</div>
                    <div class="detail-content">
                        <div class="detail-label">Zona</div>
                        <div class="detail-value">{{tableZone}}</div>
                    </div>
                </div>
                {{/if}}

                <div class="reservation-detail">
                    <div class="detail-icon">🎫</div>
                    <div class="detail-content">
                        <div class="detail-label">Código de Reserva</div>
                        <div class="detail-value">#{{reservationId}}</div>
                    </div>
                </div>
            </div>

            <div class="info-box">
                <p><strong>Importante:</strong> Por favor, llega 10 minutos antes de tu hora de reserva. Si necesitas
                    cancelar o modificar tu reserva, hazlo con al menos 2 horas de anticipación.</p>
            </div>

            <center>
                <a href="{{manageUrl}}" class="cta-button">Gestionar Reserva</a>
            </center>

            <p style="margin-top: 30px; color: #B0B0B0; font-size: 14px; text-align: center;">
                ¿Tienes alguna pregunta? Contáctanos en cualquier momento.
            </p>
        </div>

        <div class="footer">
            <p>© 2026 Restaurant. Todos los derechos reservados.</p>
            <p>
                <a href="#">Política de Privacidad</a> |
                <a href="#">Términos de Servicio</a>
            </p>
        </div>
    </div>
</body>

</html>